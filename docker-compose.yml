services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    env_file: .env
    restart: unless-stopped
    ports:
      - "3000:3000"
    volumes:
      - ./frontend/dist:/app/frontend/dist:ro
